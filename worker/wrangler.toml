name = "devforge"
main = "src/index.js"
compatibility_date = "2024-11-01"

[vars]
GITHUB_REPO = "michellemercillead-hub/devforge"
ALLOWED_ORIGIN = "https://michellemercillead-hub.github.io"

# Set the real secret with: wrangler secret put GITHUB_TOKEN
# Use a fine-grained PAT scoped to "Issues: write" on this repo only.
